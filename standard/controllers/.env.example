# Standard Controllers - Optional Local Overrides
# Copy this file to .env only if you need controller-specific overrides

# Redis configuration overrides (if needed for specific controllers)
# REDIS_STREAM_HOST=localhost
# REDIS_DATA_HOST=localhost
